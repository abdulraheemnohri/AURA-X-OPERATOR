name: Android CI

on:
  push:
    branches: ["main", "agent/**", "fix/**"]
  pull_request:
    branches: ["main"]

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "17"
      - uses: android-actions/setup-android@v4
        with:
          cmdline-tools-version: "12266719"
      - name: Install Android SDK components
        run: |
          yes | sdkmanager --licenses >/dev/null || true
          sdkmanager "platforms;android-34" "build-tools;34.0.0" "platform-tools" "cmake;3.31.6" "ndk;25.2.9519653"
      - uses: gradle/actions/setup-gradle@v6
        with:
          gradle-version: "8.2"
      - name: Toolchain diagnostics
        run: |
          java -version
          gradle --version
          adb version
      - name: Verify Android 13+ minimum SDK
        run: |
          grep -q 'minSdk = 33' app/build.gradle.kts
      - name: Assemble Android 13+ debug APK
        run: gradle --no-daemon --stacktrace assembleDebug
      - name: Run unit tests
        if: ${{ success() }}
        run: gradle --no-daemon --stacktrace testDebugUnitTest
      - name: Upload Android 13+ debug APK
        if: ${{ success() }}
        uses: actions/upload-artifact@v5
        with:
          name: aura-x-operator-android13-debug
          path: app/build/outputs/apk/debug/*.apk
          if-no-files-found: error
          retention-days: 14
